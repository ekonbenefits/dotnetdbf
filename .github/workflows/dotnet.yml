name: Build .net core

on: [push]

jobs:
  build:
    name: Test ${{ matrix.os }} for dotnet ${{ matrix.dotnet }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        dotnet: [ '10.0.x' ]
        os: [ubuntu-latest, macOS-latest]
    
    steps:
    - uses: actions/checkout@v4
      with:
        # MinVer versions from the tags, so it needs the history rather than a shallow clone.
        fetch-depth: 0
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ matrix.dotnet }}
    - name: Build with dotnet core
      run: dotnet build --configuration Release
    # KnownBugs are tests that fail because the library is wrong; each names its issue.
    # Excluded here so master stays green - `dotnet test --filter TestCategory=KnownBugs`
    # runs them deliberately.
    - name: Tests
      run: dotnet test DotNetDBF.Test/DotNetDBF.Test.csproj --configuration Release -f net10.0 --no-build --no-restore --filter "TestCategory!=KnownBugs"

  # Line and branch coverage of the two library projects, on one platform - the numbers do not
  # differ per OS, so measuring on each of them would be waste.
  coverage:
    name: Coverage
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Test with coverage
        run: |
          dotnet test DotNetDBF.Test/DotNetDBF.Test.csproj --configuration Release -f net10.0 \
            --collect:"XPlat Code Coverage" --results-directory coverage \
            --filter "TestCategory!=KnownBugs"

      # always(): a failed test step must not cost the report - that is exactly when it is
      # wanted. coverlet writes the cobertura even when tests fail.
      - name: Summarise
        if: always()
        shell: bash
        run: |
          set -e
          dotnet tool install --global dotnet-reportgenerator-globaltool
          export PATH="$PATH:$HOME/.dotnet/tools"
          reportgenerator -reports:'coverage/**/coverage.cobertura.xml' -targetdir:coverage/report \
            -reporttypes:'Cobertura;MarkdownSummaryGithub;Html' \
            -assemblyfilters:'+DotNetDBF;+DotNetDBF.Enumerable'
          cat coverage/report/SummaryGithub.md >> "$GITHUB_STEP_SUMMARY"

      - name: Upload coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage/report
